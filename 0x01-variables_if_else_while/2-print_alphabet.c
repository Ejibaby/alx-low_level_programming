#include <stdio.h>

/**
 * main - prints alphabets in lower cases
 *
 * Return: Always return 0 (sucess)
 */

int main(void)
{
	char w;

	w = 'a';
	while
		(w <= 'z')
		{
			putchar(w);
			w++;
		}
	putchar("\n");
	return (0);
}
